# Streamlit Cloud Secrets Configuration
# Copy this file to secrets.toml (for local testing) 
# Or add these secrets in Streamlit Cloud dashboard

# MongoDB Configuration
MONGODB_URI = "mongodb+srv://<username>:<password>@<cluster>.mongodb.net/"
MONGODB_DATABASE = "aqi_karachi"

# Location Configuration (Optional - defaults work for Karachi)
CITY_NAME = "Karachi"
LATITUDE = "24.8608"
LONGITUDE = "67.0104"

# Model Configuration (Optional - defaults are fine)
PREDICTION_HORIZON = "72"
TRAIN_TEST_SPLIT = "0.2"
